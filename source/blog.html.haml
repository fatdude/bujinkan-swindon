---
pageable: true
per_page: 10
title: Blog
---
-# - if paginate && num_pages > 1
-#   %p Page #{page_number} of #{num_pages}

.card-columns
  - page_articles.each_with_index do |article, i|
    .card
      = link_to thumb_tag(article.data.background, '500x250#', class: 'card-img-top'), article
      .card-body
        %h4.card-title= article.title
        %p.card-text
          %small.text-muted
            %i.icon-calendar
            = article.date.strftime('%b %e')
        %p.card-text= article.data.summary
      .card-footer
        = link_to 'Read', article

- if paginate
  %nav
    %ul.pagination.justify-content-center
      - if prev_page
        %li.page-item
          = link_to prev_page, class: 'page-link' do
            %i.icon-caret-left
            Previous page
      - if next_page
        %li.page-item
          = link_to next_page, class: 'page-link' do
            Next page
            %i.icon-caret-right
